  <%= render :template => 'shared/left_column' ,:locals => {:active => 'classes'}%>
   <% content_for :breadcrumb do -%>
       <a href="/classes">Classes</a> <span class="divider">/</span>
   <% end -%>
  <%= render :template => "shared/top_bar_new" %>
  <% content_for :main_content do -%>
       <div class="container">
        <div class="row">
          <div class="span3">
            <div class="btn-group">
            <a class="btn btn-large dropdown-toggle" data-toggle="dropdown" href="#">
              <i class="icon-user"></i> Add Classes
              <span class="caret"></span>
            </a>
            <ul class="dropdown-menu">
              <li><a href="/batch/new">Add a Class</a></li>
              <li class="divider"></li>
            </ul>
          </div>
            
          </div>
          <div class="span3">
          </div>

          <div class="span4">
            </br>
          </div>
          
        </div>
         
       </div>
        <% if current_user.type == 'Teacher' %>
          <%= render :template => 'shared/teacher_sections',:locals => {:teacher => current_user} %>
        <%else%>
          <%= render :template => 'shared/batches_index_simple',:locals => {:batches => @batches} %>
        <% end %>

  <% end -%>
